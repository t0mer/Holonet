# Claude / AI assistant context — local only, never track
CLAUDE.md
.claude/
.cursor/
.aider*

# Design / implementation / planning docs — local only
/docs/
docs/*
*.design.md
*.implementation.md

# Security scan output — local only
scans/

# Build output
/dist/
/bin/
/build/

# Go
*.exe
*.exe~
*.dll
*.so
*.dylib
*.test
*.out
coverage.*
*.coverprofile
profile.cov
go.work
go.work.sum
vendor/

# Frontend
web/node_modules/
web/dist/
web/.vite/
web/*.tsbuildinfo
*.log
npm-debug.log*

# Embedded built SPA — generated at build time (CI/Docker), keep only the
# placeholder so the go:embed compiles before any build.
/internal/webui/dist/*
!/internal/webui/dist/.gitkeep

# Local dev config / data
.env
.env.*
!.env.example
/data/
*.db
*.db-journal
*.db-wal
*.db-shm
*.sqlite
*.sqlite3

# Editor / OS
.idea/
.vscode/
*.swp
.DS_Store
